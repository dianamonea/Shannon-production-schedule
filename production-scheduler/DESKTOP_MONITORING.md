# Shannon桌面应用 - 生产调度系统监控指南

## 当前执行信息

**Session ID:** `production-scheduler-1769502931`

## 在桌面应用上查看执行结果

### 方法1：通过Session ID查看（推荐）
1. 打开Shannon桌面应用
2. 在顶部搜索框输入：`production-scheduler-1769502931`
3. 所有相关的任务都会显示出来，包括：
   - 订单分析师任务
   - 设备规划师任务
   - 物流协调员任务
   - 质量审查官任务

### 方法2：通过My Agents查看
1. 点击左侧菜单 "My Agents"
2. 查看所有正在运行的Agent任务
3. 实时监控每个Agent的执行状态

### 方法3：通过New Task提交新查询
1. 点击左侧 "New Task"
2. 在对话框中输入生产相关的查询
3. 系统会自动创建新的Session并处理

## 执行过程说明

```
┌─────────────────────────────────────────────────────────────┐
│              🏭 多Agent生产调度系统执行流                    │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Agent 1: 订单分析师  ──分析订单优先级                      │
│       └─> 输出：优先级列表、高风险订单                      │
│                ↓                                             │
│  Agent 2: 设备规划师  ──制定设备分配方案                    │
│       └─> 输出：生产计划、设备利用率                        │
│                ↓                                             │
│  Agent 3: 物流协调员  ──检查物料可行性                      │
│       └─> 输出：库存状态、采购需求                         │
│                ↓                                             │
│  Agent 4: 质量审查官  ──最终可行性评估                      │
│       └─> 输出：可行性评分、风险评估、建议                 │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## 实时监控

- **处理状态**：在桌面应用中可以看到每个Agent的处理进度
- **执行时间**：看到"⏳ 处理中"表示正在LLM处理
- **完成标记**："✅ 完成✓" 表示该Agent已生成结果
- **错误处理**：如果某个Agent失败，系统会自动使用默认数据继续

## 查看详细结果

在完成后，查看以下位置的结果文件：
```
C:\Users\Administrator\Documents\GitHub\Shannon\production-scheduler\
  └─ results_1769502931.json  (最终结果汇总)
```

## API回调URL

```
GET http://localhost:8080/api/v1/tasks/{task_id}
  - 查看单个任务状态和结果

GET http://localhost:8080/api/v1/sessions/{session_id}
  - 查看整个Session的所有任务
```

## 相关链接

- **Temporal UI**：http://localhost:8088 - 查看完整的工作流执行日志
- **Grafana监控**：http://localhost:3030 - 查看性能指标
- **Sherman API**：http://localhost:8080 - REST API端点
